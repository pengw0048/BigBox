function ci_init_start(data, done) {
    // call individual functions in sub interface of each cloud, data path pk done
    // check out one drive
    // upload to each drive iteratively
    //
    var jsonarray = JSON.parse(data);
    for (var i = 0; i < jsonarray.length; i ++) {
        var sub_data = jsonarray[i];
        if (sub_data[])
    }
}